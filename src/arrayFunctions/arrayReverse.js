/**
 * The reverse() method reverses an array in place. The first
 * array element becomes the last, and the last array element
 * becomes the first.
 */

const arrayReverseFn = arr => {
  return arr.reverse();
};

module.exports = {
  arrayReverseFn: arrayReverseFn
};
