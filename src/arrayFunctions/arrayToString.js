/**
 * The toString() method returns a string representing the specified
 * array and its elements.
 */

const toStringFn = arr => {
  return arr.toString();
};

module.exports = {
  toStringFn: toStringFn
};
